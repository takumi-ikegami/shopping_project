<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>会員登録確認</title>
</head>
<body>
    <form action="{{constant('shopping\\Bootstrap::ENTRY_URL')}}confirm.php" method="post">
        <table>
            <tr>
                <th>お名前（氏名）</th>
                <td>{{dataArr.family_name}} {{dataArr.first_name}}</td>
            </tr>
            <tr>
                <th>お名前（かな）</th>
                <td>{{dataArr.family_name_kana}} {{dataArr.first_name_kana}}</td>
            </tr>
            <tr>
                <th>郵便番号</th>
                <td>{{dataArr.zip1}}-{{dataArr.zip2}}</td>
            </tr>
            <tr>
                <th>住所</th>
                <td>{{dataArr.address}}</td>
            </tr>
            <tr>
                <th>メールアドレス</th>
                <td>{{dataArr.email}}</td>
            </tr>
            <tr>
                <th>ユーザーネーム</th>
                <td>{{dataArr.user_name}}</td>
            </tr>
            <tr>
                <th>パスワード</th>
                <td>{{replace_pass}}</td>
            </tr>
            <tr>
                <th>電話番号</th>
                <td>{{dataArr.tel1}}-{{dataArr.tel2}}-{{dataArr.tel3}}</td>
            </tr>
        </table>
        <div>
            <input type="submit" name="back" value="戻る">
            <input type="submit" name="complete" value="登録完了">
            {% for key,value in dataArr %}
                {# iterable 配列かどうか #}
                {% if value is iterable %}
                    {% for v in value %}
                        <input type="hidden" name="{{key}}[]" value="{{v}}">
                    {% endfor %}
                {% else %}
                    <input type="hidden" name="{{key}}" value="{{value}}">
                {% endif %}
            {% endfor %}
        </div>
    </form>
</body>
</html>